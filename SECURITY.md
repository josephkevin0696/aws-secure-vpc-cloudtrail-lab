# Security / Redaction Notes

Public evidence copies remove AWS account identifiers and console identity details. Resource names and RFC1918 lab CIDRs remain where they help explain the architecture.

The repository intentionally does not contain credentials, access keys, secret keys, session tokens, private keys, or configuration files with secrets.
